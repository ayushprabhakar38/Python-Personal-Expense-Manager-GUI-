# Python-Personal-Expense-Manager-GUI-
This desktop application allows you to track expenses, manage categories, search records instantly, and visualize spending through charts. All data is stored permanently in a local JSON file and loads automatically on startup.  The program uses only Python standard libraries plus matplotlib.
